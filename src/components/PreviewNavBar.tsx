import React from 'react'

type Props = {}

const PreviewNavBar = (props: Props) => {
    return (
        <header className="bg-primary">

            <nav></nav>
        </header>
    )
}

export default PreviewNavBar